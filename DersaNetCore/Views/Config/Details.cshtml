@model IEnumerable<DIOS.BusinessBase.SysParamAttr>

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<!--p>
    @Html.ActionLink("Create New", "Create")
</-->
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.predicate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.value)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.predicate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.value)
        </td>
        <td>
            @Html.ActionLink("Edit", "EditDetail", new { id=item.sys_param_attr }) 
        </td>
    </tr>
}

</table>
<div>
    <br>
    @Html.ActionLink("Back to List", "Index")
</div>
